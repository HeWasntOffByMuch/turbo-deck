// Renderer entry point. Populated in the "minimal renderer + input" phase
// (specs/005-renderer-input.md) — this stub exists only so the build
// pipeline has something to compile in Phase 0.
const app = document.getElementById('app');
if (app) {
  app.textContent = 'turbo-deck: renderer not implemented yet';
}
